<h1>Hamburger creation</h1>

<form action="{{ path('admin_hamburger_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_hamburger') }}">
            Back to the list
        </a>
    </li>
</ul>
